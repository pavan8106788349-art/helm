apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: {{ default "ClusterIP" (get .Values.service "type") }}
    - port: {{ .Values.service.port | default 80 }}
      targetPort: 80
  selector:
    component: frontend